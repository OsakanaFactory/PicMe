# ============================================
# PicMe バックエンド 環境変数テンプレート
# ============================================
# このファイルを .env にコピーして値を設定してください
# cp .env.example .env

# === データベース ===
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/picme_db
SPRING_DATASOURCE_USERNAME=picme_user
SPRING_DATASOURCE_PASSWORD=picme_password

# === JWT ===
# 本番環境では必ず強力なランダム文字列に変更してください（256bit以上推奨）
JWT_SECRET=your-secret-key-change-this-in-production-at-least-256-bits

# === Cloudinary（画像アップロード） ===
# 取得方法: https://console.cloudinary.com/ → Dashboard
# 未設定の場合: 画像アップロード機能が無効になります
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# === Stripe（決済） ===
# 取得方法: https://dashboard.stripe.com/apikeys
# 未設定の場合: サブスクリプション機能が無効になります
STRIPE_API_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_STARTER=
STRIPE_PRICE_PRO=
STRIPE_PRICE_STUDIO=

# === SendGrid（メール送信） ===
# 取得方法: https://app.sendgrid.com/settings/api_keys
# 未設定の場合: メールがコンソールに出力されます（開発用フォールバック）
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=noreply@picme.com
SENDGRID_FROM_NAME=PicMe

# === アプリケーション設定 ===
APP_FRONTEND_URL=http://localhost:3001
CORS_ALLOWED_ORIGINS=http://localhost:3001
SERVER_PORT=8080
